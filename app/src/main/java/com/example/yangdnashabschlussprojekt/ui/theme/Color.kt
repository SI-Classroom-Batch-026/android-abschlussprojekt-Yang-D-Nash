package com.example.yangdnashabschlussprojekt.ui.theme

import androidx.compose.ui.graphics.Color

// --- DARK MODE (Neon Deep Sea 2026) ---
val NeonCyan = Color(0xFF00F2FF)      // Hauptfarbe (Scanner/Objekte)
val NeonMagenta = Color(0xFFFF00FF)   // Akzentfarbe (Text/Übersetzung)
val DeepSpaceBlack = Color(0xFF030507) // Hintergrund Ende
val DeepSpaceCyan = Color(0xFF001A1C)  // Hintergrund Start (Der "Deep Sea" Glow)
val VividPurple = Color(0xFF7000FF)    // Sekundär-Akzent
val SurfaceDark = Color(0xFF0E121A)    // Karten/Sheets
val GlassWhite = Color(0xFFF0F0F0)     // Primärer Text
val CyberGray = Color(0xFF8E8E93)      // Sekundärer Text

// --- LIGHT MODE ---
val LightPrimary = Color(0xFF0061A4)
val LightBgStart = Color(0xFFD1E4FF)   // Hintergrund Start Light
val LightBgEnd = Color(0xFFF8F9FF)     // Hintergrund Ende Light